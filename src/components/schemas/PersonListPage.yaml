---
description: A result page used for a list of people.
allOf:
  - $ref: "./ResultPage.yaml"
  - type: object
    properties:
      results:
        type: array
        items:
          $ref: "./PersonListItem.yaml"
    required:
      - results
