---
description: A result page used for a list of TV shows.
allOf:
  - $ref: "./ResultPage.yaml"
  - type: object
    properties:
      results:
        type: array
        items:
          $ref: "./PersonListItem.yaml"
    required:
      - results
