---
description: A result page for a search for movies.
allOf:
  - $ref: "./ResultPage.yaml"
  - type: object
    required:
      - results
    properties:
      results:
        type: array
        items:
          $ref: "./MovieListItem.yaml"
