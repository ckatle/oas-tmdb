---
description: A result page used for a list of movies ordered by popularity or rating.
allOf:
  - $ref: "./ResultPage.yaml"
  - type: object
    required:
      - results
    properties:
      results:
        type: array
        items:
          $ref: "./MovieListItem.yaml"
