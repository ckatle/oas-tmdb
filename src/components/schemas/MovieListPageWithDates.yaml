---
description: A result page with min and max dates used for a list of movies that are currently in theatres or upcoming
  movies.
allOf:
  - type: object
    properties:
      dates:
        type: object
        properties:
          maximum:
            type: string
            format: date
          minimum:
            type: string
            format: date
        required:
          - maximum
          - minimum
    required:
      - dates
  - $ref: "./MovieListPage.yaml"
